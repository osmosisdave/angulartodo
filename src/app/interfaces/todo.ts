
// instead of using an object array, we defined the array as a todo array and speified the interface here

export interface Todo {
    id: number,
    title: string,
    completed: boolean,
    editing: boolean,
}
